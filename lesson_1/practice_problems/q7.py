# What will the following output?

words = ['ant', 'bear', 'cat']
selected_words = []
for word in words:
    if len(word) > 3:
        selected_words.append(word)

print(selected_words)

# Output:

# Performing a selection using a loop and the selection criteria; len(word) > 3

# ['bear']